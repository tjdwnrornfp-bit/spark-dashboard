# v10.7.0 — 관리자 작업 정정

- 관리자 프로그램 접수 목록의 데스크톱/모바일 행에 작은 `수정` 버튼 추가. 일반회원과 중간관리자는 사용할 수 없음.
- 상호명, 키워드, 플레이스 URL/MID, 수량, 기간, 메모 및 시작 전 작업의 시작일 정정. 서버 영향 확인 후 적용하며 사유 2~500자 필수.
- 기존 주문 단가와 정산 단계 단가를 사용. 금액 불변이면 payment_steps 전체와 활성화 시각·상태 보존.
- 입금대기 금액 증감 및 입금완료 증가 지원. 확인 내역은 유지하고 차액만 추가. 미확인 단계는 ID를 유지해 재계산하고 기존 quote는 만료. 기존 데이터 삭제 없음.
- 확인된 정산의 감소, 구동중/정지 금액 변경, 과거 종료일, 보관 상태, 복잡한 프로그램 변경 정산 이력은 자동 적용 차단.
- `order.corrected`: 관리자, 사유, 전후 주문, 금융 영향, 차액, 추가 단계 생성 여부, 시각 기록.
- `order.created`: manual/excel/admin_manual/admin_excel, 수량·기간·날짜·단가·공급가·VAT·총액·등록자·실행자·프로그램·상호명·MID 추가. 과거 감사로그는 변경하지 않음.
- 스파크/스파크+의 일일수량 1~10 및 구동일수 30 이상이면 반대 입력 의심 경고. 단건/일반 엑셀/관리자 직접·엑셀 모두 명시적 확인으로 계속할 수 있음. SparkS/SparkS+는 제외.
- 기존 최신순/오래된순, 중간관리자 권한, current group, SparkS 계열 gauge 제외, 리소스별 부분 갱신 유지.

검증: typecheck, build, test:assignment, test:correction 통과. 핵심 DB 규칙은 격리된 PostgreSQL 호환 테스트에서 검증. 운영에서는 신규 RPC 권한·스키마·기존 함수 보존과 지정 주문 정정을 검증. 모든 운영 계정으로 end-to-end 정산/프로그램 변경을 실행한 것은 아님.

운영 적용: `v10_7_admin_order_correction` (Supabase 기록 `20260907050841`), 앱 스키마 `v10.7.0`.
