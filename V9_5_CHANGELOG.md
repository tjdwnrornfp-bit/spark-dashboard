# SPARK v9.5 변경 내역

## 회원 계정 정리

관리자 회원관리 화면에 `계정 영구 삭제` 기능을 추가했습니다.

삭제 가능한 계정은 다음 조건을 모두 만족해야 합니다.

- 관리자 계정이 아님
- 직접 접수한 작업이 없음
- 추천 관계로 연결된 주문 이력이 없음
- payment_steps 정산 참여 이력이 없음
- sponsor/manager로 연결된 하위 회원이 없음
- 작성한 공지가 없음
- 정산 견적 이력이 없음
- 정산 묶음 이력이 없음

삭제 가능한 경우 Supabase Edge Function이 Auth 사용자를 hard delete 합니다. `profiles`, `member_contacts`, 해당 회원 알림은 기존 FK cascade 정책에 따라 정리됩니다. 작업/정산 이력이 있는 회원은 삭제하지 않습니다.

삭제 완료 후 같은 아이디로 다시 가입할 수 있습니다.

## 보안

- 삭제 버튼은 관리자 화면에만 표시합니다.
- 브라우저에는 service_role 키를 넣지 않습니다.
- Edge Function이 호출자의 access token과 관리자 권한을 다시 확인합니다.
- 삭제 직전에 서버에서 삭제 가능 여부를 다시 검사합니다.
- 관리자 본인 및 관리자 계정 삭제를 차단합니다.
- 삭제 기록은 `audit_logs`에 `member.permanent_delete`로 기록합니다.
- 전화번호와 비밀번호는 삭제 감사 기록에 저장하지 않습니다.
