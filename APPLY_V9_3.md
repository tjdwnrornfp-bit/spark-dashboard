# SPARK v9.3.0 적용 가이드

## 1. 적용 전
Supabase SQL Editor에서 `supabase/preflight_v9_3.sql`을 실행해 회원/주문/정산 건수를 기록합니다.

## 2. DB 업데이트
`supabase/update_v9_3_operations_manager.sql` 전체를 새 SQL 창에서 실행합니다.

이 SQL은 기존 운영 데이터를 삭제하지 않습니다. 회원가입 트리거를 v9.3용으로 교체하므로, 적용 후에는 예전 `repair_signup_v8.sql`을 다시 실행하지 마세요.

## 3. 검증
`supabase/verify_v9_3.sql`을 실행합니다.
- 버전 `v9.3.0`
- `is_operations_manager`, `manager_id`, `manager_username` 컬럼 존재
- `review_member_v93` 존재
- `on_auth_user_created` 트리거 활성
- `invalid_dual_links = 0`

## 4. 프런트 배포
`npm run build` 후 GitHub main 브랜치에 push합니다.

## 5. 중간관리자 생성
1. 중간관리자로 사용할 별도 계정을 추천/관리 코드 없이 회원가입합니다.
2. 관리자 → 회원관리 → 해당 계정 검토
3. 회원 유형을 `중간관리자`로 선택
4. 관리자용 그룹명을 입력하고 승인
5. 중간관리자가 로그인하면 `관리 코드`가 표시됩니다.

작업·정산 이력이 있는 기존 운영 계정은 중간관리자로 전환하지 않습니다.

## 6. 하위 대행사 가입
1. 대행사가 회원가입 화면에서 중간관리자의 아이디 또는 관리 코드를 입력합니다.
2. 중간관리자의 회원관리 화면에 승인대기로 표시됩니다.
3. 중간관리자가 3개 프로그램 단가를 입력하고 승인합니다.
4. 승인된 대행사의 정산 화면에는 관리자 계좌가 표시됩니다.
5. 해당 대행사가 작업을 접수하면 payment step은 대행사 → 관리자로 직접 생성됩니다.

## 중간관리자 권한
가능: 관리 대행사 조회, 승인/반려, 프로그램별 단가 지정, 알림/공지/내 정보 확인

불가: 작업 접수, 정산 입금확인, 정산 계좌 설정, 전체 회원 조회, 운영기록, 공지 등록/삭제, 관리자 설정
