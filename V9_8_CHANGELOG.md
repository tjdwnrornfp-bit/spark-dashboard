# SPARK v9.8.0

## 관리자 통합 엑셀

- 스파크 / 스파크+ / 스파크s를 복수 선택해 한 번에 다운로드할 수 있습니다.
- 입금대기 / 입금완료 / 구동중 / 정지 / 만료 상태를 복수 선택할 수 있습니다.
- 그룹명과 등록자 아이디로 결과를 좁힐 수 있습니다.
- 조건에 맞는 운영 작업 전체 또는 목록에서 직접 체크한 작업만 다운로드할 수 있습니다.
- 결과는 `통합작업` 단일 시트로 생성합니다.
- 열은 등록자, 그룹명, 프로그램, 대표키워드, 미드값, 상호명, 플레이스URL, 적용단가, 일일수량, 시작날짜, 종료날짜, 구동일 수, 상태 순서입니다.
- 프로그램 값은 `스파크`, `스파크+`, `스파크s`로 고정합니다.
- 시작날짜와 종료날짜는 실제 Excel 숫자 날짜 셀이고 `yyyy-mm-dd` 서식을 적용합니다.
- 기존 프로그램별 선택 엑셀 다운로드는 유지합니다.

## 관리자 회원 비밀번호 재설정

- 관리자 회원관리에서 일반 회원의 새 비밀번호를 직접 입력할 수 있습니다.
- 암호학적 난수로 임시 비밀번호를 생성하고 재설정 후 한 번 복사할 수 있습니다.
- 현재 비밀번호 조회 기능은 제공하지 않습니다.
- `reset-member-password` Edge Function이 요청 JWT와 활성·승인 관리자 권한을 다시 확인합니다.
- `service_role`과 `auth.admin.updateUserById`는 Edge Function 내부에서만 사용합니다.
- 관리자 계정은 이 기능의 대상에서 제외하고 기존 `내 정보`에서 현재 비밀번호 확인 후 변경합니다.
- 감사로그에는 작업 종류, 대상 회원, 실행 관리자, 실행 시각만 기록하며 비밀번호는 저장하지 않습니다.

## 유지보수

- 앱 버전을 `9.8.0`으로 올렸습니다.
- 기존에 `package.json`과 맞지 않던 `package-lock.json`을 현재 고정 버전에 맞게 동기화했습니다.
