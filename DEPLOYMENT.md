# SPARK Dashboard v8 배포 메모

## 반영된 주요 변경
- 엑셀 일괄 접수 시작일 오프셋 문제 수정
- 작업접수 탭을 `스파크 접수 / 스파크 + 접수 / 스파크S 접수`로 분리
- 프로그램별 대시보드 박스 추가
- 프로그램별 단가(스파크 / 스파크 + / 스파크S) 개별 관리
- 만료 / 정지 / 입금대기 작업 삭제 기능 추가
- 입금 확인 완료 알림은 **최종 관리자 확인 완료 시 1회만** 발송되도록 조정
- 테스트용 로컬 더미 작업 데이터 제거
- 접수 탭 좌측 아이콘 교체

## Supabase 적용 순서
1. 기존 v7이 적용된 DB에서 `supabase/update_v8.sql` 실행
2. 검증용으로 `supabase/verify_v8.sql` 실행
3. 기존 테스트 작업을 모두 비우려면 필요 시 `supabase/clear_test_orders_v8.sql` 실행
4. 프론트 소스 배포

## 참고
- 프론트에서 엑셀 날짜는 로컬 날짜 기준으로 `YYYY-MM-DD`로 변환되도록 수정됨
- 프로그램별 정산은 주문 생성 시점의 프로그램별 단가를 스냅샷으로 저장하므로 이후 단가를 바꿔도 기존 정산이 뒤틀리지 않음


## v8.1 UI 보완
- 좌측 상단 SPARK 로고 마크 확대
- 세 프로그램 아이콘을 동일한 실제 표시 크기로 재정리
- 좌측 접수 메뉴 아이콘 확대
- 대시보드 프로그램별 카드 가독성 개선
- 내 정보 화면을 프로필·추천 코드·프로그램 단가·계좌·보안 카드 구조로 재배치
- `tsconfig.node.json`의 `noEmit` 설정 포함
