# SPARK v10.11.0 — 성능·조회 안정성 개선

기준: v10.10.0 / 8dd41609f2b014d12b6de43cb6bec22cb01e6caf.

## 적용 순서

1. `npm ci && npm run verify`로 기존 기능과 추가 성능 회귀 테스트를 확인합니다.
2. `supabase/migrations/20260921095732_v10_11_performance_read_paths.sql`을 적용합니다. 이 파일명의 버전은 운영 Supabase에 기록된 마이그레이션 버전과 일치합니다. 이미 적용된 환경에는 다시 적용하지 않습니다.
3. 동일한 소스 커밋을 main에 반영합니다. Vercel의 buildCommand도 `npm run verify`이므로 테스트 실패 시 빌드가 중단됩니다.
4. 기존 로그인 계정으로 조회·검색·엑셀·정산 화면을 확인합니다. 운영 브라우저 성능은 별도 측정 대상입니다.

## 변경

- App 전체 5초 타이머 제거. 마운트된 구동 게이지만 공통 타이머를 구독하며 숨긴 탭에서는 멈춥니다. 한국 날짜 변경은 자정과 탭 복귀 시 확인합니다.
- 날짜 포맷터 재사용 및 행별 오늘 날짜 중복 계산 제거. 날짜 표시는 한국 시간대를 사용합니다.
- 작업표는 서버 필터·정렬·50건 페이지 조회. PC 테이블과 모바일 카드는 화면 크기에 맞는 하나만 마운트합니다.
- 대시보드는 전체 건수·금액 서버 집계, 최근 7건, 회원 구동 목록 50건만 조회합니다. 금액은 기존 정산 요약 함수를 그대로 사용합니다.
- 정산 서버 모드의 로컬 전수 집계를 제거하고 출금 정산은 50건씩 조회합니다. 하단 전체 작업 결제 상태는 펼칠 때 조회합니다.
- 검색과 비동기 조회에 요청 식별·효과 정리·결과 키 검사를 적용합니다. 진행 중인 동일 조회는 공유하고 데이터 변경 후 이전 요청을 재사용하지 않습니다.
- Realtime은 열린 화면 조회를 무효화하며 전체 주문·정산 스냅샷을 다시 내려받지 않습니다. 재연결·온라인 복귀·탭 복귀 시 최신 상태를 확인합니다. 작업 상태 변경과 보관 처리도 Realtime 도착을 기다리지 않고 화면을 갱신합니다.
- 알림 배지는 전체 미읽음 집계입니다. 전체·미읽음 목록 모두 커서로 다음 페이지를 조회하며 모두 읽음은 로드한 100건이 아닌 본인의 실제 전체 미읽음을 처리합니다.
- 주문 엑셀은 화면 페이지와 분리하여 전체를 1,000건씩 조회합니다. 전체 행 수·중복·수정 버전을 검증하며 여러 페이지 선택을 유지합니다. 조회 중 변경이 감지되면 재시도 안내를 표시합니다.
- 기존 전체 정산 스냅샷 함수도 1,000건씩 끝까지 조회하도록 보완합니다. 일반 화면은 이 스냅샷을 사용하지 않습니다.

## DB 변경 및 보존 범위

새 RPC 6개는 SECURITY INVOKER, 고정 search_path, 활성 승인 회원 검사 및 기존 RLS를 사용합니다. 정산 금액·입금 확인 순서·프로그램 전환·취소·작업 시작/종료일·구동 정책을 변경하지 않습니다.

외래키 인덱스 7개와 미읽음 알림 부분 인덱스 1개를 추가합니다. `touch_updated_at`의 search_path를 고정합니다. 점검 대상 구버전/트리거 함수의 PUBLIC·anon 실행 권한만 회수하고 authenticated 및 내부 트리거 동작을 유지합니다. 기존 인덱스·함수·테이블·운영 주문·정산 데이터는 삭제하지 않습니다.

비밀번호 최소 길이·고정 해시 로그인·유출 비밀번호 보호 정책은 이번 배포에서 변경하지 않습니다. 로그인 기능에 필요한 authenticated SECURITY DEFINER 경고를 일괄 차단하지 않습니다.

## 검증

- 기존 접수·담당 지정·작업 정정·회원 수정·선택·업체 폴더·하위 작업 회귀 테스트.
- 격리 PGlite에서 작업 1천/5천/1만 건, 역할별 권한·보관 제외·기존 정산 요약 일치·현재 그룹·날짜/상태/프로그램 필터.
- 1,000건 초과 전체 엑셀 조회, 여러 페이지 선택, 데이터 변경 시 내보내기 중단.
- 230개 미읽음 알림의 100/100/30 커서 조회, 중간 읽음 처리 후 누락 방지, 사용자 간 격리.
- 훅 테스트에서 오래된 응답 무시, 동시 요청 공유·변경 후 무효화, 게이지 50개당 공유 타이머 1개 및 숨김/해제 정리.
- 컴포넌트 훅 테스트에서 1만 건을 전달해도 PC/모바일 중 한 레이아웃만 50행 생성. 서로 다른 페이지 선택 2건의 엑셀 출력 확인.
- TypeScript 및 Vite 프로덕션 빌드. GitHub CI는 자동 커밋 권한 없이 읽기 전용으로 검증하며 검증 로그만 아티팩트로 보관합니다.
- 운영 마이그레이션 전후 주문·정산 데이터 해시와 합계 비교. 읽기 전용 트랜잭션으로 관리자 페이지 제한·대시보드 기존 정산 합계 일치 확인.

위 테스트는 실제 로그인 브라우저 응답 시간이나 전체 사이트 속도 배수를 측정한 결과가 아닙니다. 로컬 브라우저 접근 제한 때문에 운영 화면의 시각·실측 검증은 수행하지 못했습니다. 기존 Vite 대형 번들 경고는 별도 코드 분할 개선 대상으로 남깁니다.

## 되돌리기

프런트 장애 시 기존 v10.10.0 커밋으로 되돌립니다. 새 DB 읽기 함수와 인덱스는 구버전 프런트와 병행할 수 있으므로 긴급 복구를 위해 운영 데이터를 삭제하거나 정산 정책을 변경하지 않습니다. 자동 테스트는 운영 DB 접속 정보 없이 격리 데이터베이스에서만 실행합니다.
