# v10.10.0 — 작업 그룹/폴더 보기

- 중간관리자 관리 작업: 기존 대행사별/전체 작업 보기를 유지하고 현재 그룹명 표시·검색·엑셀을 추가했습니다. 조회 범위는 manager_id 그대로입니다.
- 대행사·총판 전용 **하위 작업** 메뉴: 현재 sponsor_id의 전체 descendant를 `현재 그룹 → 대행사 → 작업`으로 조회합니다. 본인 작업은 기존 접수 메뉴에 유지합니다. 관리자와 중간관리자에게는 이 메뉴를 노출하지 않습니다.
- 그룹·대행사 요약은 각각 20개씩 서버 페이지 처리합니다. 작업은 대행사를 펼칠 때 50개씩 조회합니다. 전체 작업 보기도 서버 페이지 처리합니다.
- 프로그램/진행중/작업 상태/전체 정산 단계 상태/시작일/검색/정렬, 행 클릭·Shift 범위 선택, 현재 페이지 전체 선택, 대행사 필터 전체 선택을 지원합니다.
- 선택/필터 전체/현재 그룹/현재 대행사 엑셀을 지원합니다. 전체 엑셀은 500개 단위로 서버 권한을 재검사하며 조회하고, 현재 그룹명 열을 추가합니다.
- 하위 폴더 금액은 현재 사용자가 payee인 미확인 payment_steps의 **내 수령대기**입니다. 선행 단계 대기도 포함하며, 주문 총액이나 전체 체인의 합계가 아닙니다. 단계가 없으면 금액을 추정하지 않습니다. 정산상태는 작업 전체 단계 상태입니다.
- 기존 RLS, 쓰기/입금확인/회원수정/프로그램변경/archive/reversal 함수, sponsor_id, 정산 체인, 과거 주문 그룹 snapshot은 변경하지 않습니다. 신규 RPC 6개와 비공개 계층 조회 helper만 추가합니다.
- 접힌 폴더의 조회 결과를 재사용합니다. 요약 revision이 달라진 대행사의 상세만 갱신하며 필터·정렬·펼침 상태를 유지합니다. Realtime은 기존 420ms 병합을 사용합니다. 기존 RLS에서 이벤트를 받을 수 없는 깊은 계층은 활성 화면에서 30초마다 **현재 페이지 요약**을 확인하고, 화면 복귀/새로고침도 지원합니다. 따라서 서버 조회는 즉시 현재 그룹 기준이며 다른 세션의 깊은 계층 변경은 자동 표시까지 최대 약 30초 걸릴 수 있습니다.
- 모바일 작업 카드, 긴 이름 말줄임/툴팁, 하위 회원이 없는 계정의 정상 빈 상태를 지원합니다.

검증: `npm run typecheck`, `npm run build`, 기존 전체 회귀 테스트, `npm run test:downline`. 깊은 4단계/순환 계층, manager·sponsor 교차 관계, 무권한 ID, 현재 그룹 변경 및 snapshot 보존, 금액/페이지/엑셀 일치 검증을 포함합니다.
