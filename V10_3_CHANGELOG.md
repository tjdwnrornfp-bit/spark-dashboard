# SPARK v10.3.0

- 중간관리자 대시보드를 회원 승인 정보 대신 관리 대행사의 운영·정산 현황 중심으로 재구성했습니다.
- 상단에 관리 대행사 수, 전체 작업 수, 총 정산금액, 정산대기 금액, 정산완료 금액을 표시하고 구동중/입금대기/입금완료/만료/정지 건수를 보조 지표로 제공합니다.
- `관리 대행사별 현황`은 12개 단위 서버 페이지네이션, 대행사 아이디 검색, 정산대기금액/전체작업/구동중/아이디 정렬을 지원합니다. 카드에는 전체 작업, 주요 작업상태, 정산대기 및 정산완료 금액만 간결하게 표시합니다.
- 카드 전체 또는 `작업 보기`를 누르면 기존 v10.2 `관리 작업` 화면에 대행사 필터가 적용됩니다. 정산 금액을 누르면 대행사와 정산상태 필터가 함께 적용됩니다.
- 신규 `get_manager_dashboard_summary_v103()` 및 `get_manager_agency_overview_v103(...)` RPC는 `SECURITY DEFINER`, 빈 `search_path`, 승인·활성 중간관리자 검증 및 `profiles.manager_id = auth.uid()` 범위를 사용합니다.
- 정산금액은 현재 회원 단가로 재계산하지 않습니다. 주문 등록 대행사가 payer인 실제 `payment_steps`만 합산해 상위 추천 정산 step의 중복을 막고, `confirmed_at` 기준으로 대기/완료를 분리합니다. 프로그램 변경 보정과 입금확인 취소도 이 실제 step 상태에 포함됩니다.
- 중간관리자에게 상태 변경, 프로그램 변경, 입금확인/취소, 보관/복원/삭제 권한을 추가하지 않았습니다. 기존 관리 작업 조회·필터·Excel 기능은 그대로 유지됩니다.
