# SPARK v9 운영 안정성 업데이트

## 적용 범위

- 작업 실제 삭제를 중단하고 **보관/복원** 방식으로 전환
- 작업 상태 변경 사유 필수 입력
- 동시 수정 충돌 방지용 `lock_version` 적용
- 회원 단가 수정 시 기존 하위회원 단가 역전 방지
- 회원 동시 수정 충돌 감지
- 정산 단계를 아래 단계부터 순서대로 확인
- 최종 입금완료 알림 중복 방지
- 보관된 작업은 자동 구동·자동 만료·정산 집계에서 제외
- 작업·회원·입금확인 감사 로그 저장
- 관리자 전용 `운영기록` 화면 추가
- 관리자 수, 정산 누락, 상태 불일치, Cron 상태 점검

## 데이터 보존 정책

작업의 보관 버튼은 DB 행을 삭제하지 않습니다.

보관 시 유지되는 항목:

- 주문 원본
- 접수 당시 단가
- 결제금액
- 정산 단계
- 입금확인 시각과 확인자
- 기존 알림
- 상태 변경·보관 감사기록

보관 작업은 대시보드와 정산 집계에서는 제외되고 각 프로그램 접수 탭의 `보관함`에서 확인할 수 있습니다. 복원은 관리자만 가능합니다.

## 배포 순서

1. Supabase 백업 또는 프로젝트 복제
2. `supabase/preflight_v9.sql` 실행 및 결과 저장
3. `supabase/update_v9_stability.sql` 전체 실행
4. `supabase/verify_v9_stability.sql` 실행
5. 프런트 파일 배포
6. 관리자 `운영기록` 탭에서 모든 경고가 0인지 확인
7. 테스트 주문 1건으로 접수 → 입금확인 → 구동 → 보관 → 복원 확인

## 주의

- `schema.sql`, `schema_fresh_v6.sql`, `update_v6.sql`, `update_v7.sql`, `update_v8.sql`은 다시 실행하지 않습니다.
- DB 업데이트 전에 v9 프런트를 먼저 배포하면 새 RPC가 없어 상태변경·보관 기능이 실패합니다.
- v9 SQL 적용 후 구버전 프런트의 삭제 버튼은 실제 삭제가 아니라 호환용 보관으로 동작합니다.
- 운영기록은 관리자에게만 보입니다.
