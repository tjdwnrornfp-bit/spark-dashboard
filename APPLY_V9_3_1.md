# v9.3.1 적용

Supabase SQL 변경은 없습니다.

교체 파일:

- `src/features/OrdersPage.tsx`
- `package.json`
- `V9_3_1_CHANGELOG.md`
- `APPLY_V9_3_1.md`

적용 후:

```cmd
npm run build
git add .
git commit -m "Improve order Excel export"
git pull --rebase origin main
git push origin main
```
