# SPARK v9.4 적용 가이드

## 주요 변경

### 회원가입 전화번호
- 회원가입 시 전화번호 필수 입력
- 숫자 8~15자리 검증
- 전화번호는 `profiles`와 분리된 `member_contacts`에 저장
- 일반 대행사/총판/중간관리자는 다른 회원 전화번호를 직접 조회할 수 없음
- 관리자 회원관리에서만 전화번호 표시
- 기존 회원은 전화번호 데이터가 없으므로 `-` 표시

### 관리자 업체별 입금확인
- 관리자 정산의 입금대기 목록을 등록 업체별 카드로 그룹화
- 카드 상단에 등록 그룹/계정, 총 예정 입금액, 확인 가능 금액 표시
- 스파크/스파크+/스파크S별 건수와 금액 표시
- 상세 행에서는 작업 업체명과 입금자 열을 제거하고 프로그램, 주문번호/MID, 단가, 예정입금액, 상태를 표시
- `이 업체 전체 입금확인`은 현재 페이지가 아니라 현재 필터에 해당하는 해당 업체의 확인 가능한 전체 정산 단계를 서버에서 다시 계산하여 처리
- 일부 작업만 받은 경우 같은 업체 카드 안에서 체크박스로 필요한 건만 선택 후 확인 가능
- 관리자 수동 선택은 한 업체씩만 처리하도록 제한

## 적용 순서

1. Supabase SQL Editor에서 `supabase/preflight_v9_4.sql` 실행
2. `supabase/update_v9_4_member_phone_company_settlement.sql` 전체 실행
3. `supabase/verify_v9_4.sql` 실행
4. 프런트 패치 덮어쓰기
5. `npm run build`
6. GitHub push / Vercel 배포

## Git 명령

```cmd
npm run build
git add .
git commit -m "Add signup phone and company settlement cards"
git pull --rebase origin main
git push origin main
```

## 데이터 보존
이번 SQL은 기존 회원/주문/정산/게이지/알림 데이터를 삭제하지 않습니다. 기존 정산 단계와 일괄 입금확인 이력도 유지됩니다.

## 적용 후 테스트
1. 새 테스트 계정 회원가입 시 전화번호 입력
2. 관리자 회원관리에서 전화번호 표시 확인
3. 일반 대행사/총판/중간관리자 화면에 전화번호가 추가 노출되지 않는지 확인
4. 서로 다른 등록자 계정에서 작업 여러 건 접수
5. 관리자 정산에서 등록 업체별 카드 분리 확인
6. 카드의 프로그램별 건수/금액과 총 예정 입금액 확인
7. 한 업체에서 일부 작업만 체크 후 `선택 작업 입금확인`
8. 다른 업체를 체크하면 기존 업체 선택이 해제되고 새 업체 선택만 유지되는지 확인
9. `이 업체 전체 입금확인` 금액 팝업 확인
10. 입금확인 후 다음 정산 단계/주문 상태가 기존처럼 정상 진행되는지 확인
