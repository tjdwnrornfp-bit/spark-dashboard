# SPARK v10.0.0

- Added `spark_s_plus` as the fourth program with UI label `스파크S+`.
- Added the profile price `spark_s_plus_price_per_shot`; every existing profile starts at 40원 and new profiles inherit a 40원 default.
- Added individual and bulk intake, price snapshots, VAT settlement chains, member approval/editing, hierarchy validation, filters, dashboards, company summaries, and single/bulk program transfer support.
- Added the code-based Spark S Plus icon, using the existing Spark S family shape with a distinct plus mark.
- Kept Spark/Spark+ in 타 dashboards and separated Spark S and Spark S Plus as 건 metrics without progress gauges.
- Added Spark S Plus to selected, program-specific, and integrated admin workbooks. The exported program value is exactly `스파크s+`; columns A-M and Excel date cell formatting are unchanged.
- Added v10 preflight and verification SQL, including the 40원 backfill and RPC/constraint checks.
