# SPARK v10.2.0

- 중간관리자 사이드바에 전용 읽기 화면 `관리 작업`을 추가했습니다. 기존 관리자 접수·정산 화면과 쓰기 컴포넌트는 재사용하지 않습니다.
- `profiles.manager_id = auth.uid()`인 관리 대행사의 활성(미보관) 작업만 서버에서 조회합니다. 대행사·프로그램·작업상태·정산상태·검색·시작일 범위 필터와 50건 단위 서버 페이지네이션을 제공합니다.
- 정산 상태는 payment step 전체 확인 여부를 기준으로 `정산대기`, `부분완료`, `정산완료`로 계산합니다. `program_transfer_state = payment_pending`과 `settlement_reversal_pending`은 사유가 표시되는 `정산대기`로 우선 분류합니다.
- 선택한 작업과 현재 필터 전체를 각각 Excel로 내려받을 수 있습니다. 필터 전체는 RPC를 500건 단위로 반복 호출하며, 화면의 현재 50건에 제한되지 않습니다.
- Excel의 날짜 셀은 실제 날짜 형식(`yyyy-mm-dd`)이고 단가·수량·구동일·금액은 숫자 셀로 유지합니다.
- 중간관리자 대시보드에 관리 대행사 수, 전체 접수 건수, 구동중 건수, 정산대기 건수와 `관리 작업 보기` 이동 버튼을 추가했습니다.
- 신규 RPC 세 개는 `SECURITY DEFINER`와 빈 `search_path`를 사용하고 승인·활성 중간관리자만 실행할 수 있습니다. 관리자/일반 대행사 호출은 `42501`로 거절됩니다.
- 주문 상태 변경, 프로그램 변경, 입금확인·취소, 보관·복원·삭제 RPC의 권한이나 grant는 변경하지 않았습니다.
