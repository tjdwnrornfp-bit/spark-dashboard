# SPARK v9.2 정산 운영 업데이트 적용

## 변경 범위

대행사·총판의 대시보드, 접수, 회원관리, 내 정보, 공지사항 화면은 기존 v9.1과 동일합니다.
정산 화면의 `하위 대행사 입금 확인` 영역에만 다음 기능이 추가됩니다.

- 입금자·등록자·그룹·업체·MID·주문번호·프로그램·상태·시작일 필터
- 서버 기준 50건 페이지 조회
- 현재 페이지 선택 및 검색 결과 전체 선택
- 선택 건수와 예정금액 실시간 표시
- 입금자별 실제 입금액·통장 입금자명 최종 대조
- 입금자별 정산 묶음번호 생성
- 일괄 입금확인 이력

기존 개별 `입금확인` 버튼과 순차 정산 규칙은 유지됩니다.

## 1. 적용 전 읽기 전용 점검

먼저 아래 파일을 실행하고 회원·주문·정산 단계 건수를 캡처해 둡니다.

```text
supabase/preflight_v9_2.sql
```

## 2. Supabase 적용

SQL Editor의 새 쿼리에서 아래 파일 전체를 실행합니다.

```text
supabase/update_v9_2_settlement_batches.sql
```

실행 중 RLS 경고가 표시되면 `Run and enable RLS`를 선택합니다.
이 SQL은 기존 회원·작업·정산 확인 데이터를 삭제하거나 초기화하지 않습니다.

## 3. 검증

```text
supabase/verify_v9_2_settlement_batches.sql
```

정상 기준:

- `version = v9.2.0`
- 신규 테이블 네 개가 모두 `true`
- 신규 RPC 여섯 개가 모두 `true`
- 신규 테이블의 `rls_enabled = true`

## 4. 프런트 적용

패치 ZIP의 파일을 기존 프로젝트에 덮어씁니다. `.git`, `.env.local`은 유지합니다.

## 5. 빌드 및 배포

```cmd
npm install
npm run build
git add .
git commit -m "Add settlement batch confirmation workflow"
git pull --rebase origin main
git push origin main
```

## 6. 테스트

1. 하위 계정 두 곳에서 서로 다른 작업을 접수합니다.
2. 하위 단계부터 입금확인을 완료합니다.
3. 수취인 정산 화면에서 입금자·등록자 필터가 구분되는지 확인합니다.
4. 일부 행만 선택하고 선택 건수·금액을 확인합니다.
5. `일괄 입금확인`에서 실제 입금액을 예정금액과 다르게 입력해 차단되는지 확인합니다.
6. 금액을 일치시키고 통장 입금자명을 입력해 처리합니다.
7. 입금자별로 별도 정산 묶음번호가 생성되는지 확인합니다.
8. 다음 상위 단계가 활성화되는지 확인합니다.
9. 관리자의 최종 일괄 확인 후 주문이 `입금완료`로 바뀌는지 확인합니다.
10. 보관 작업이 검색 결과와 금액에서 제외되는지 확인합니다.
