# SPARK v9.10.0

## 관리자 일괄 프로그램 변경

- 관리자 작업 목록 상단의 `선택 엑셀` / `통합 엑셀` 옆에 전역 `프로그램 변경` 버튼을 배치했습니다.
- 데스크톱 표와 모바일 카드의 체크박스로 선택한 작업을 한 번에 스파크 / 스파크+ / 스파크s 중 하나로 변경할 수 있습니다.
- 기존 행별 프로그램 변경 버튼은 제거했으며 상태 변경, 보관, 복원 기능은 그대로 유지합니다.
- 선택은 프로그램 화면을 이동해도 유지되어 서로 다른 현재 프로그램·등록자·그룹의 작업을 함께 처리할 수 있습니다.
- 일괄 모달에 선택 수, 현재 프로그램별 건수, 대상 프로그램, 예상 추가금·차감 합계, 동일 프로그램 제외 수, 변경 불가 수와 주문별 사유를 표시합니다.
- 좁은 화면에서는 상단 3개 버튼과 모달 요약이 자연스럽게 줄바꿈됩니다.

## 서버 처리와 안전성

- `preview_bulk_order_program_transfer_v910` RPC가 모든 선택 주문을 서버에서 미리 계산합니다.
- `transfer_bulk_order_program_v910` RPC가 브라우저 반복 호출 없이 한 요청에서 주문 ID 순서대로 처리합니다.
- 각 주문은 PostgreSQL 하위 트랜잭션으로 분리되어 실패 주문의 변경·정산·알림·감사로그는 전부 롤백되고, 다른 성공 주문은 유지됩니다.
- 실제 주문 변경은 검증된 v9.9 단일 RPC를 호출하므로 등록자 대상 프로그램 현재 단가, 정산 체인 참여자별 단가, 확인 이력 보존, 차액 단계, 환불·과납 차단 규칙을 그대로 적용합니다.
- 전달된 `lock_version`을 주문마다 다시 검사해 미리보기 이후 동시 수정된 주문만 실패 처리합니다.
- 대상 프로그램과 이미 같은 주문은 자동 제외됩니다.
- 공통 사유는 2~500자로 한 번 입력하며 성공한 모든 주문의 감사로그에 actor, 사유, 전후 프로그램·단가·총액, 차액과 함께 기록됩니다.
- 앱 및 스키마 버전을 `9.10.0`으로 올렸습니다.
