# v10.6.0 — 관리자 작업 부여 / 현재 그룹명 표시

기준 main: `88cb103` (`Restrict managed work tab to operations managers`). v10.5 성능 최적화와 v10.4 관리자 배정 기능을 유지합니다.

- 관리자 프로그램 접수 화면에 작업 부여, 엑셀 일괄 부여, 선택 엑셀, 통합 엑셀 액션을 배치했습니다.
- 승인·활성 상태의 일반 대행사/총판만 대상이 됩니다. 중간관리자와 관리자는 대상에서 제외됩니다.
- 작업 소유자와 생성 시점 스냅샷은 대상 회원으로 저장합니다. 관리자 입력자와 수동/엑셀 구분은 `order.admin_assigned` 감사로그에 기록합니다.
- 기존 `create_order_v10` 생성 로직을 접근 제한된 공통 함수로 추출해 일반 접수와 관리자 부여가 동일한 주문번호·정산 체인·알림·기본값을 사용합니다. 네 프로그램의 대상 회원 단가를 서버에서 다시 조회하며 0원은 차단합니다.
- 엑셀은 업로드 전용 A~I 양식, 최대 500건, 실행 전 행별 검증, 정상/오류 건수, 행별 실행 결과를 제공합니다. 프런트는 50건 단위로 전송하고 서버는 각 행의 예외를 독립적으로 롤백합니다.
- 요청 UUID와 원본 행 번호로 성공 주문을 기억합니다. 응답이 유실된 요청을 재실행해도 성공 주문과 감사로그가 중복 생성되지 않습니다.
- 관리자 및 일반 회원의 프로그램별 운영/보관 목록을 최신 접수순으로 표시합니다. 기존 선택/통합 Excel 파일의 오름차순 정렬은 유지합니다.
- 관리자 목록·대시보드·Excel·정산 그룹/검색·업체 현황은 현재 `profiles.group_name`을 사용합니다. 그룹명을 공란으로 바꿔도 과거 그룹명으로 되돌아가지 않습니다. 기존 주문과 정산 이력 스냅샷은 보존합니다.
- 회원/주문 snapshot 조회가 Data API의 단일 페이지 한도에서 잘리지 않도록 1,000건씩 이어서 읽습니다. 관리자 회원 정보는 재사용하고, 부여 직후 주문은 응답으로 병합하며 정산 단계/알림과 관련 서버 집계만 갱신합니다.
- 단가 0을 과거 기본단가로 잘못 대체하던 프런트 fallback을 nullish fallback으로 정정했습니다.

검증: `npm run typecheck`, `npm run build`, `npm run test:assignment`. 로컬 PostgreSQL(PGlite)에서 네 프로그램, 소유권, 두 단계 정산/관리자 직결, 권한, 대상/단가/입력 검증, 500건, 후속 정산 오류의 행 롤백, 감사, 재실행, 일반 개별/대량 접수, 현재/공란 그룹, 그룹별 정산 선택, 과거 스냅샷 보존을 검증합니다. 실제 고객 데이터는 테스트 fixture에 포함하지 않습니다.

운영 DB: 2026-09-03 신규 마이그레이션 `v10_6_admin_order_assignment` 적용 완료. 운영 이력 버전 `20260903095441`, 앱 스키마 `v10.6.0`. 적용 전후 주문 298, payment_steps 305, 회원 57, audit_logs 1,258건 및 회원 관계/주문 그룹 스냅샷 해시가 동일했습니다. 운영 성공 주문 생성 없이 차단 경로 9개와 현재 그룹 조회를 검증했습니다.
