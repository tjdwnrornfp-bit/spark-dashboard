# SPARK v10.0.0 적용 가이드

## 적용 순서

1. 운영 Supabase 프로젝트와 최근 `app_schema_versions`가 맞는지 확인합니다.
2. SQL Editor에서 `supabase/preflight_v10.sql`을 실행합니다. v9.10 프로그램 변경 RPC가 모두 `true`여야 합니다.
3. `supabase/migrations/20260826120000_v10_spark_s_plus.sql` 전체를 한 번 실행합니다.
4. 같은 세션에서 관리자가 단가를 수정하기 전에 `supabase/verify_v10.sql`을 실행합니다.
5. 애플리케이션 v10.0.0을 배포하고 회원관리, 스파크S+ 개별 접수, 대량접수, 선택/통합 엑셀, 단일/일괄 프로그램 변경을 확인합니다.

## 안전성 및 롤백 주의

- 마이그레이션은 기존 주문·정산·회원 데이터를 삭제하지 않습니다.
- 새 컬럼 생성 시 기존 모든 프로필에 40원이 한 번 backfill됩니다. 이후 재실행은 이미 수정한 단가를 40원으로 덮어쓰지 않습니다.
- `spark_s_plus` 주문이 생성된 뒤에는 프로그램 제약이나 가격 컬럼을 제거하면 안 됩니다. 문제가 생기면 먼저 앱을 이전 버전으로 되돌리고 DB 구조는 유지한 채 원인을 수정합니다.
- 프로그램 변경의 확인 완료 정산 이력, 차액 정산, optimistic locking, 감사로그 정책은 v9.9/v9.10 함수를 확장해 유지합니다.
- Edge Function 변경은 필요하지 않습니다.
