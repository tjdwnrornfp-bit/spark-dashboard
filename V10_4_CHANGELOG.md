# SPARK v10.4.0

- 관리자 `회원관리`에 현재 관리 담당을 명확히 표시하고 개별 `중간관리자 배정`/`관리 담당 변경`/`관리자 직속 해제`를 추가했습니다.
- 체크박스로 여러 대행사·총판을 선택해 동일 중간관리자 또는 관리자 직속으로 일괄 변경할 수 있으며, 회원별 성공/실패 결과를 화면에 표시합니다.
- 기존 `sponsor_id`가 있는 회원을 선택하면 추천·정산 관계가 유지된다는 경고를 표시합니다.
- 배정 후보는 승인 완료·활성 중간관리자만 노출합니다. admin, 중간관리자 본인 계정, 비활성·반려 계정 등은 관리 대상 선택에서 제외합니다.
- 신규 `admin_assign_member_manager_v104(...)`와 `admin_bulk_assign_member_manager_v104(...)` RPC는 승인·활성 admin을 서버에서 검증하고, 대상 및 새 중간관리자를 다시 검증하며, 자기 배정과 stale update를 차단합니다.
- 성공한 각 회원마다 `member.manager_changed` 감사로그에 변경 전/후 담당자, 유지된 sponsor, 실행자, 사유, 변경 시각을 기록합니다.
- 변경은 `profiles.manager_id`, `profiles.manager_username`, `updated_at`에 한정됩니다. 추천·정산 계층과 기존 주문/정산 데이터는 변경하지 않습니다.
- 기존 v10.2 관리 작업/Excel과 v10.3 대시보드는 현재 `manager_id` 관계를 사용하므로 재배정 즉시 기존 과거 작업까지 새 중간관리자 범위에 반영됩니다.
