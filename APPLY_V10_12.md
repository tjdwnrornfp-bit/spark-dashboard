# SPARK v10.12.0 — 시작일 접수 제한

관리자 메뉴의 **접수 제한 설정**에서 시작일·종료일·안내 문구·사용 여부를 저장합니다. 여러 기간을 등록할 수 있으며 양 끝 날짜를 포함합니다. 실제 연휴 날짜는 배포 시 등록하지 않습니다.

## 동작

- 모든 프로그램(스파크, 스파크+, 스파크S, 스파크S+)에 공통 적용합니다. 개별·일반 엑셀·관리자 작업부여·관리자 엑셀을 포함하며 관리자 강제 통과 옵션은 없습니다.
- 접수 버튼을 누르는 날짜가 아니라 **주문의 시작일**만 검사합니다. 기존 작업을 자동 취소·정지하거나 구동 기간을 바꾸지 않습니다. 차단 기간을 가로질러 구동하는 작업도 시작일이 허용되면 이 정책으로는 막지 않습니다.
- 달력에서 차단 날짜를 비활성화합니다. 직접 입력·엑셀·오래 열린 화면·구버전 RPC·허용 날짜로 접수 후 시작일 수정도 DB 트리거가 검사합니다.
- 기존 작업의 시작일이 그대로라면 메모·입금확인·정산·상태 변경을 이 정책 때문에 차단하지 않습니다. 기존 수정 가능 상태/정산 정책은 그대로 적용됩니다.
- 엑셀 사전검사에서 제한된 행이 있으면 행 번호·상호명·사유를 표시하고 제출을 막습니다. 서버는 일반 엑셀 호출 전체를 롤백하며 관리자 엑셀도 해당 RPC 내 제한 날짜를 실행 전에 검사합니다. 관리자는 기존 50행 단위 실행을 유지하므로 도중 설정 변경/네트워크 실패 전에 이미 성공한 앞 묶음은 유지하며 결과를 표시합니다. 파일을 다시 선택할 때는 이미 성공한 행을 빼야 합니다.
- 설정 조회가 실패하거나 응답 형식이 잘못되면 신규 접수·시작일 변경을 허용하지 않습니다. 설정 변경·연결 복구·탭 복귀 시 설정을 갱신합니다. DB 검사가 최종 기준입니다.
- 기간이 겹치면 하나라도 활성인 제한에 포함된 날짜는 계속 차단합니다. 해제는 해당 기간 수정에서 사용 체크를 끈 뒤 저장합니다.
- 저장 전에 해당 기간의 기존 미보관 작업 수를 보여 주며, 변경 이력은 운영기록에 남깁니다. 동시 편집은 버전 검사로 덮어쓰지 못하게 합니다.

## DB 구조와 보존

`order_start_restrictions`는 RLS 및 승인된 회원 읽기만 허용합니다. 일반 API의 직접 INSERT/UPDATE/DELETE 권한은 없습니다. 공개 RPC는 SECURITY INVOKER이며 실제 설정 저장은 비공개 스키마의 관리자 검사 함수만 수행합니다. 내부 상태 행은 기본 거부입니다.

주문 INSERT/시작일 실변경 트리거는 내부 설정 행을 공유 잠금으로 읽습니다. 설정 저장은 배타적 행 잠금과 revision 증가를 사용하므로 이미 진행 중인 허용 접수는 먼저 끝나고, 저장 후 검사하는 접수는 새 정책을 따릅니다. 오래된 REPEATABLE READ 스냅샷은 직렬화 오류로 실패하며 이전 정책으로 통과하지 않습니다.

마이그레이션은 새 설정 테이블/함수/트리거와 관리자 엑셀 사전검사만 추가합니다. 운영 주문·정산 행은 변경하지 않으며 비밀번호/인증 정책과 기존 인덱스는 변경하지 않습니다. 주문/정산의 마이그레이션 직전·직후 해시를 확인합니다.

## 검증 및 배포

`npm run verify`는 기존 전체 회귀 테스트와 `test:start-date-restrictions` 및 프로덕션 빌드를 포함합니다. 새 DB 테스트는 빈 정책, 양 끝 날짜, 네 프로그램, 역할별 단건/엑셀, 실패 시 주문·정산·알림 롤백, 시작일 수정 우회, 기존 날짜 보존, 겹친 기간/해제, 관리자 권한과 변경 이력을 확인합니다.

격리 브라우저 검증은 `tests/start-date-restrictions-browser.mjs`로 만들어진 모의 Supabase 화면과 `tests/start-date-restrictions-browser.py`를 사용합니다. 실제 운영 로그인을 대신하는 검증은 아닙니다. `tests/start-date-restrictions-concurrency.py`는 별도 PostgreSQL 서비스의 합성 데이터에서만 실행합니다.

순서: 후보 브랜치 테스트 → DB 마이그레이션 → 운영 읽기 검증 → main 병합 → main CI 및 Vercel 배포 확인. 기능 반영 후 관리자가 원하는 날짜를 설정해야 실제 제한이 시작됩니다.

## 복구

프런트 문제는 직전 v10.11.0으로 복구할 수 있습니다. DB 제한은 구버전 클라이언트에도 계속 적용됩니다. 업무상 제한을 해제하려면 활성 기간을 관리 화면에서 사용 안 함으로 저장합니다. 주문·정산 테이블 삭제나 트리거 임의 비활성화는 하지 않습니다.
