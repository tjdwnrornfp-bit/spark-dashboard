# SPARK v10.1.0

- 관리자 `업체별 입금 확인` 행에 상호명과 구동 시작일을 추가했습니다. 관리자 표에는 주문번호와 MID를 노출하지 않습니다.
- 확인완료 행에 관리자 전용 `확인 취소` 작업과 사유 필수 모달을 추가했습니다.
- `admin_reverse_payment_confirmation_v101` RPC가 관리자 권한, 확인시각, 주문 lock version, 후속 확인 단계, 프로그램 변경 이력을 한 트랜잭션에서 재검증합니다.
- 시작 전 `입금완료` 주문은 `입금대기`로 복구합니다. `구동중`, `정지`, `만료` 주문은 운영 상태를 유지하고 정산 확인만 역전합니다. 시작일이 도래했지만 아직 `입금완료`인 애매한 상태는 안전을 위해 차단합니다.
- `PAYMENT_CONFIRM_REVERSED` 감사 이벤트에 payment step/order, payer/payee, 원 확인시각·확인자, 관리자, 사유, 역전시각, 관련 배치 이력을 기록합니다.
- 기존 settlement batch/item은 삭제·수정하지 않습니다. 역전된 배치 항목을 재확인하면 새 배치 항목이 추가되어 확인·역전·재확인 이력이 보존됩니다.
- 운영 상태 유지형 역전을 `settlement_reversal_pending`으로 구분하고, 재확인 완료 시 자동 해제하여 운영 진단의 오탐을 방지합니다.
- 정산 목록 RPC에 주문 상태와 optimistic lock version을 포함해 취소 모달과 서버 동시성 검증이 같은 스냅샷을 사용합니다.

