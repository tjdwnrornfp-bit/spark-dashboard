# SPARK v9.9.0

## 관리자 작업 프로그램 변경

- 관리자만 운영 작업의 프로그램을 `spark` / `spark_plus` / `spark_s` 사이에서 변경할 수 있습니다.
- 확인 모달은 서버에서 다시 계산한 기존/새 단가, 공급가, VAT, 총액, 차액, 정산 확인 건수, 상태 영향을 표시합니다.
- 주문 번호와 등록자, 그룹, 상호명, 키워드, URL, MID, 수량, 구동일수, 시작일, 종료일 등 작업 정보는 유지합니다.
- 주문 단가는 관리자나 기존 주문 단가가 아니라 주문 등록자의 대상 프로그램 **현재 활성 승인 단가**로 갱신합니다.
- 등록자 또는 정산 참여자의 대상 프로그램 단가가 0원이거나 미설정이면 변경을 차단합니다.
- 스파크/스파크+는 `타`, 스파크S는 `건`으로 표시하며 변경 후 목록과 통합 엑셀에는 새 프로그램이 반영됩니다.
- 스파크S로 이동한 작업에는 기존 정책대로 진행 게이지를 표시하지 않습니다.

## 정산 보존 정책

- 정산 확인이 0건이면 기존 미확인 단계를 대상 프로그램의 참여자별 현재 승인 단가로 전부 재구성합니다.
- 확인 이력이 있으면 확인된 `payment_steps`와 `settlement_batch_items`를 수정하거나 삭제하지 않습니다.
- 참여자별 새 목표 공급가/VAT에서 이미 확인된 공급가/VAT를 뺀 잔액만 `program_adjustment` 단계로 추가합니다.
- 주문 총액이 내려가거나 어느 참여자 단계에서 과납이 생기는 변경은 환불/차감 자동 처리를 하지 않고 차단합니다.
- 입금완료 작업에 추가금이 생기면 `입금대기`로 돌아가고, 마지막 보정 단계 확인 후 기존 입금완료 흐름으로 복귀합니다.
- 구동중 작업은 구동을 유지하고 `추가입금/변경 정산 대기` 표시만 추가합니다.
- 정지·만료 작업도 운영 상태를 유지한 채 변경 정산을 별도로 처리합니다.
- 관리자 업체 현황과 작업별 관리자 정산액은 기존 단계와 보정 단계를 모두 합산해 표시합니다.

## 보안·감사·동시성

- `preview_order_program_transfer_v99`와 `transfer_order_program_v99` SECURITY DEFINER RPC가 관리자 권한, 주문 버전, 가격, 정산 상태를 서버에서 재검증합니다.
- 실행 시 결제 견적, 결제 단계, 주문, 관련 가격 프로필을 정해진 순서로 잠가 일괄 입금확인과의 충돌을 방지합니다.
- 활성 견적에 포함된 미확인 단계는 견적을 만료시킨 뒤에만 안전하게 교체합니다.
- `order_program_transfers`에 변경 전후 값과 정산 방식을 보존합니다.
- 감사로그에는 `order_id`, 변경 전후 프로그램·단가·총액, 차액, 실행 관리자, 사유, 시각을 기록합니다.
- 앱 버전을 `9.9.0`으로 올렸습니다.
