# v10.8.0

- 일반 회원의 본인 입금대기·미보관·입금확인 0단계 작업에 수정/프로그램 변경 추가. 상호명, 대표키워드, 플레이스 URL/MID, 일일수량, 구동일수, 시작일, 메모를 수정할 수 있습니다.
- 서버에서 회원 활성/승인/역할, 소유권, 상태, 모든 payment_steps의 confirmed_at, expected lock_version을 다시 검증합니다. 중간관리자와 다른 회원의 수정은 차단합니다.
- 프로그램 유지 시 기존 주문 및 정산 단계의 단가 snapshot 유지. 프로그램 변경 시 대상 프로그램의 현재 승인 단가를 조회합니다. 0원 단가 차단. 기존 step ID와 payer/payee 관계를 보존해 미확인 금액을 재계산하고 기존 견적을 만료시킵니다. 데이터 삭제 없음.
- 변경 종료일은 오늘 이전 불가. 시작일 변경은 활성화 이력이 없는 작업에서 익일 이후로 가능합니다. 이상값 경고와 확인 체크를 적용했습니다. 복잡한 변경정산 이력·단가 불일치는 관리자 확인을 안내합니다.
- `order.member_corrected` / `order.member_program_changed` 감사로그에 변경 전후, 사유, 사용자, 프로그램, 금액 기록. 추가 알림을 대량 생성하지 않습니다. 성공 후 주문 로컬 반영 및 paymentSteps/notifications 부분 갱신.
- 관리자 데스크톱 행/모바일 카드와 중간관리자 행 클릭으로 선택 토글. Shift 범위 선택, 체크박스 Shift/키보드 선택, 선택 강조/개수/해제 지원. 버튼·링크·입력·select·label·data-no-row-select 제외.
- 중간관리자는 현재 페이지 내에서 선택하며 페이지·필터·정렬 변경 시 선택/anchor 초기화. 갱신 응답의 순서 역전 방지와 선택된 행 데이터 동기화 포함.
- 빠른 상태 버튼과 select는 단일 `orderStatus` 필터를 공유합니다. `in_progress`는 정확히 입금대기+입금완료. 버튼 문구는 `진행중(입금대기+입금완료)`입니다. 칩별 추가 건수 조회는 생략하고 현재 필터 결과 건수를 표시합니다.
- 신규 v108 관리 작업 RPC는 상태 배열과 정렬을 서버에서 처리합니다. 기본 업무 우선순위는 입금대기→입금완료→구동중→정지→만료. 같은 상태는 created_at DESC, id DESC입니다. 상태변경 감사로그는 존재하지만 조회 때마다 집계하지 않도록 이번 버전은 접수 시각을 사용합니다.
- 최신/오래된 접수순, 시작일 빠른순 지원. 서버 pagination 유지, 필터 전체 Excel도 동일한 RPC/필터/정렬 사용. 기존 대행사·프로그램·정산·날짜·검색 유지.
- 관리자 고급 수정/정산보정·프로그램 전환, 회원 인라인 편집, 접수표 컬럼 축소, 현재 그룹 표시, 권한 메뉴, 부분 refresh를 유지합니다.

검증: `npm run typecheck`, `npm run build`, `npm run test:assignment`, `npm run test:correction`, `npm run test:member-edit`, `npm run test:selection`. 운영 검증은 읽기 전용/권한 차단 호출만 수행합니다.
